<x-layouts.app title="Páginas — Usina Docs"><h1>Páginas</h1><p><a class="button" href="{{ route('admin.pages.create') }}">Criar página</a></p>@foreach($pages as $page)<article class="card" style="margin:16px 0"><h2>{{ $page->slug }}</h2>@foreach($page->localizations as $localization)<p>{{ $localization->language->native_name }} · {{ $localization->publishedRevision ? 'publicada' : 'pendente' }} @if($localization->publishedRevision)<a href="{{ route('pages.show',['locale'=>$localization->language->route_key,'slug'=>$localization->slug]) }}">ver</a>@endif</p>@endforeach<p><a class="button alt" href="{{ route('admin.pages.translations.create',$page) }}">Adicionar tradução</a></p></article>@endforeach</x-layouts.app>
